import PreviousProjects from "@/components/pages/project/project-past/PreviousProjects";
import React from "react";

export const metadata = {
  title: "Previous Projects",
  description:
    "Displays list of previously registered projects on the Sustainability Hub portal.",
};

const page = () => {
  return <PreviousProjects />;
};

export default page;
